{
  "name": "webpack-modules-export",
  "version": "1.0.2",
  "description": "",
  "main": "webpackPlugin.js",
  "scripts": {
    "test": "mocha",
    "npm@latest": "npm version patch && cnpm publish && git push && git push origin --tags",
    "npm@beta": "npm version prerelease && npm publish --tag=beta && git push && git push origin --tags"
  },
  "keywords": [
    "webpack",
    "modules",
    "export"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "mocha": "^5.0.5"
  }
}
